% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missings.R
\name{ess_as_factor}
\alias{ess_as_factor}
\title{Turn coded ESS columns into factors}
\usage{
ess_as_factor(
  dt,
  round = NULL,
  variables = NULL,
  kind = "integrated",
  keep_missing = FALSE,
  max_levels = 50L,
  cache = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{dt}{A \code{data.table}, normally from \code{\link[=ess_round]{ess_round()}}.}

\item{round}{The ESS round the data came from. When \code{NULL} (the default) it
is taken from the \code{essround} column.}

\item{variables}{Optionally, restrict recoding to these columns. Defaults to
every column of \code{dt}.}

\item{kind}{Which data file of the round, as named by \code{\link[=ess_file_kinds]{ess_file_kinds()}}.}

\item{keep_missing}{If \code{TRUE}, missing codes become factor levels — "Refusal"
and so on — instead of \code{NA}.}

\item{max_levels}{Columns with more distinct codes than this are left as they
are. Guards against turning a near-continuous variable into a factor with
hundreds of levels.}

\item{cache}{If \code{TRUE} (the default), reuse catalogue answers for the rest of
the session.}

\item{quiet}{If \code{TRUE}, suppress the progress bar and the summary.}
}
\value{
\code{dt}, invisibly and modified in place.
}
\description{
Replaces the numeric or string codes of categorical variables with labelled
factors, using the value labels from the metadata catalogue.
}
\details{
Only variables with a code list are converted; continuous ones such as \code{agea}
and identifiers such as \code{idno} are left alone. Codes flagged as missing
become \code{NA} rather than a factor level, unless \code{keep_missing = TRUE}.

Like \code{\link[=ess_recode_missings]{ess_recode_missings()}}, this modifies \code{dt} \strong{by reference}. Pass
\code{data.table::copy(dt)} if you want to keep the codes.
}
\examples{
\dontrun{
dt <- ess_round(11, select = c("essround", "cntry", "gndr", "ppltrst"))
ess_as_factor(dt, variables = "gndr")
dt[, .N, by = gndr]
}

}
