% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variables.R
\name{ess_variables}
\alias{ess_variables}
\title{Variables in an ESS data file}
\usage{
ess_variables(
  round = NULL,
  kind = "integrated",
  datafile_id = NULL,
  datafile_version = NULL,
  cache = TRUE
)
}
\arguments{
\item{round}{An ESS round number.}

\item{kind}{Which data file of that round, as named by \code{\link[=ess_file_kinds]{ess_file_kinds()}}.
Defaults to the main integrated file.}

\item{datafile_id, datafile_version}{Alternatively, identify the data file
directly. Both are returned by \code{\link[=ess_data_files]{ess_data_files()}}.}

\item{cache}{If \code{TRUE} (the default), reuse catalogue answers for the rest
of the session.}
}
\value{
A \code{data.table} with columns \code{variable_name}, \code{variable_label},
\code{group_name}, \code{group_label}, \code{role}, \code{variable_id} and
\code{variable_version}.
}
\description{
Lists a data file's variables with their labels and the variable group they
belong to, taken from the metadata catalogue. No survey data is downloaded.
}
\details{
Use this to find the variable names to pass to the \code{select} argument of
\code{\link[=ess_round]{ess_round()}} or \code{\link[=ess_data]{ess_data()}}, which is much faster than reading all
600-plus columns of an integrated file.

The list is a superset of the data file's columns. A few entries are templates
whose name ends in \code{xx}, standing for a country code — \code{prtvtxx} documents the
party-voted-for question, which appears in the data as \code{prtvtcnl} for the
Netherlands, \code{prtvtdde} for Germany and so on. Others, such as the higher
household-grid slots, exist in the questionnaire but have no column in a given
round. Passing such a name to \code{select} is an error naming the column, since it
is checked against the file itself.
}
\examples{
\dontrun{
vars <- ess_variables(11)
nrow(vars)

# Find the trust variables.
vars[grepl("trust", variable_label, ignore.case = TRUE)]

# Then read only those columns.
dt <- ess_round(11, select = c("cntry", vars[1:5]$variable_name))
}

}
