% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missings.R
\name{ess_missing_codes}
\alias{ess_missing_codes}
\title{Codes the ESS treats as missing}
\usage{
ess_missing_codes(
  round,
  variables = NULL,
  kind = "integrated",
  cache = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{round}{An ESS round number.}

\item{variables}{Optionally, restrict to these variable names. Strongly
recommended: without it, metadata for every variable in the file is read.}

\item{kind}{Which data file of the round, as named by \code{\link[=ess_file_kinds]{ess_file_kinds()}}.}

\item{cache}{If \code{TRUE} (the default), reuse catalogue answers for the rest of
the session.}

\item{quiet}{If \code{TRUE}, suppress the progress bar.}
}
\value{
A \code{data.table} with columns \code{variable_name}, \code{value} and \code{label}.
}
\description{
Reports, per variable, which response codes stand for "Refusal", "Don't
know", "No answer", "Not applicable" and similar. The codes differ between
variables — \code{ppltrst} uses 77, 88 and 99 while \code{agea} uses 999 — which is why
they are read from the metadata rather than assumed.
}
\details{
These are the codes that \code{recode_missings = TRUE} converts to \code{NA} on the
server, and that \code{\link[=ess_recode_missings]{ess_recode_missings()}} acts on locally.
}
\examples{
\dontrun{
ess_missing_codes(11, variables = c("ppltrst", "agea", "gndr"))
}

}
