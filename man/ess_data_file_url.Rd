% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/http.R
\name{ess_data_file_url}
\alias{ess_data_file_url}
\title{Build the URL of an ESS data file}
\usage{
ess_data_file_url(
  doi,
  user_id = ess_user_id(),
  format = "parquet",
  recode_missings = TRUE
)
}
\arguments{
\item{doi}{The DOI of a \emph{data file}. Study-level DOIs such as
\code{10.21338/NSD-ESS10-2020} are rejected by the API; see
\code{\link[=ess_data_files]{ess_data_files()}} for data file DOIs.}

\item{user_id}{Your ESS API user ID. Defaults to \code{\link[=ess_user_id]{ess_user_id()}}.}

\item{format}{One of \code{"parquet"} (the default), \code{"csv"}, \code{"sav"} or
\code{"dta"}.}

\item{recode_missings}{If \code{TRUE} (the default), ask the API to convert
values flagged as missing in the metadata — "Not applicable", "No answer"
and so on — into system missing values.

The API treats its \code{recodeMissingValues} parameter as a flag, so any value
at all switches recoding \emph{on}. This package therefore omits the parameter
entirely when \code{recode_missings = FALSE}, which is the only way to turn it
off.}
}
\value{
The URL as a length-one character vector.
}
\description{
Returns the URL that \code{\link[=ess_download_file]{ess_download_file()}} fetches. Exported so that a
download can be handed to another tool, or inspected when a request is
behaving unexpectedly.
}
\examples{
ess_data_file_url(
  "10.21338/ess11e04_2",
  user_id = "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
)

}
