% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{ess_all_rounds}
\alias{ess_all_rounds}
\title{Download every published ESS round}
\usage{
ess_all_rounds(
  kind = "integrated",
  select = NULL,
  recode_missings = TRUE,
  format = "parquet",
  user_id = ess_user_id(),
  use_cache = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{kind}{Which data file of each round to download, as named by
\code{\link[=ess_file_kinds]{ess_file_kinds()}}. Defaults to the main integrated file.}

\item{select}{Optionally, a character vector of variables to read. Only
these columns are decoded from the parquet file, which is considerably
faster than reading all of them. \code{\link[=ess_variables]{ess_variables()}} lists the names.}

\item{recode_missings}{If \code{TRUE} (the default), values flagged as missing in
the ESS metadata — "Not applicable", "Refusal", "Don't know" and so on —
arrive as \code{NA}. Set to \code{FALSE} to keep the original codes.}

\item{format}{The transfer format, \code{"parquet"} by default. See
\code{\link[=ess_data]{ess_data()}} for the alternatives.}

\item{user_id}{Your ESS API user ID. Defaults to \code{\link[=ess_user_id]{ess_user_id()}}.}

\item{use_cache}{If \code{TRUE} (the default), reuse downloads already on disk and
keep this one, where the cache is available. See \code{\link[=ess_cache_dir]{ess_cache_dir()}}.}

\item{quiet}{If \code{TRUE}, suppress progress and status messages.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} with every round stacked, distinguished by
the \code{essround} column.
}
\description{
Downloads all rounds listed by \code{\link[=ess_rounds]{ess_rounds()}} and stacks them into one
\code{data.table}.
}
\details{
This transfers a lot of data — the integrated files come to several hundred
megabytes — so it warns before starting unless \code{quiet = TRUE}. Passing
\code{select} cuts the transfer down substantially, and the cache means the cost
is paid only once.
}
\examples{
\dontrun{
# A few variables across the whole series.
trust <- ess_all_rounds(select = c("essround", "cntry", "ppltrst", "pspwght"))
trust[, .N, by = essround]

# Everything, which is a large download.
all <- ess_all_rounds()
}

}
