% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missings.R
\name{ess_value_labels}
\alias{ess_value_labels}
\title{Value labels for ESS variables}
\usage{
ess_value_labels(
  round,
  variables = NULL,
  kind = "integrated",
  cache = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{round}{An ESS round number.}

\item{variables}{Optionally, restrict to these variable names. Strongly
recommended: without it, metadata for every variable in the file is read.}

\item{kind}{Which data file of the round, as named by \code{\link[=ess_file_kinds]{ess_file_kinds()}}.}

\item{cache}{If \code{TRUE} (the default), reuse catalogue answers for the rest of
the session.}

\item{quiet}{If \code{TRUE}, suppress the progress bar.}
}
\value{
A \code{data.table} with columns \code{variable_name}, \code{value}, \code{label} and
\code{is_missing}.
}
\description{
Returns the response categories of each variable: the codes that appear in
the data and what they mean.
}
\details{
Parquet files hold codes only — a \code{gndr} of 1 or 2, not "Male" or "Female" —
so this is where the labels come from. That is a real difference from the
earlier \code{essurvey} package, which read SPSS files carrying their own labels.
}
\examples{
\dontrun{
labs <- ess_value_labels(11, variables = "gndr")
labs
}

}
