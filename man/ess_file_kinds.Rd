% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{ess_file_kinds}
\alias{ess_file_kinds}
\title{Data file kinds}
\usage{
ess_file_kinds()
}
\value{
A \code{data.table} with columns \code{file_kind}, \code{file_tag} and
\code{description}.
}
\description{
The ESS publishes several data files per round. \code{ess_file_kinds()} lists the
kinds this package recognises, as accepted by the \code{kind} argument of
\code{\link[=ess_round]{ess_round()}} and \code{\link[=ess_data_files]{ess_data_files()}}.
}
\details{
Not every kind exists for every round — most exist for only a few.
\code{\link[=ess_data_files]{ess_data_files()}} shows what a given round actually has.

Alongside these there is a \code{"country"} kind, covering the supplementary files
the ESS publishes for countries held out of an integrated file, such as
\code{ESS4AT} (Austria, whose fieldwork ran late) or \code{ESS3LV} (Latvia, which has no
design weights). Those files are identified by an ISO country code rather than
a questionnaire abbreviation, so they share one kind; the \code{file_country}
column of \code{\link[=ess_data_files]{ess_data_files()}} says which country each one is for.
}
\examples{
ess_file_kinds()

}
