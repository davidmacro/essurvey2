% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{ess_country}
\alias{ess_country}
\title{Download one country's ESS data}
\usage{
ess_country(
  country,
  rounds = NULL,
  select = NULL,
  recode_missings = TRUE,
  format = "parquet",
  user_id = ess_user_id(),
  use_cache = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{country}{An ISO 3166-1 alpha-2 country code, such as \code{"NL"}. Case is
ignored. \code{\link[=ess_countries]{ess_countries()}} lists the valid codes.}

\item{rounds}{Round numbers. When \code{NULL} (the default) every round the
country took part in is used, as reported by \code{\link[=ess_country_rounds]{ess_country_rounds()}}.}

\item{select}{Optionally, a character vector of variables to read. Only
these columns are decoded from the parquet file, which is considerably
faster than reading all of them. \code{\link[=ess_variables]{ess_variables()}} lists the names.}

\item{recode_missings}{If \code{TRUE} (the default), values flagged as missing in
the ESS metadata — "Not applicable", "Refusal", "Don't know" and so on —
arrive as \code{NA}. Set to \code{FALSE} to keep the original codes.}

\item{format}{The transfer format, \code{"parquet"} by default. See
\code{\link[=ess_data]{ess_data()}} for the alternatives.}

\item{user_id}{Your ESS API user ID. Defaults to \code{\link[=ess_user_id]{ess_user_id()}}.}

\item{use_cache}{If \code{TRUE} (the default), reuse downloads already on disk and
keep this one, where the cache is available. See \code{\link[=ess_cache_dir]{ess_cache_dir()}}.}

\item{quiet}{If \code{TRUE}, suppress progress and status messages.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} holding only that country's rows. When
\code{select} is given, \code{cntry} is read regardless, since it is needed to
filter, and it is kept in the result.
}
\description{
Returns the respondents from a single country across the requested rounds.
}
\details{
The current ESS API publishes integrated files only — there are no
per-country downloads any more, unlike the endpoints the earlier \code{essurvey}
package used. Each requested round is therefore downloaded in full and then
filtered on its \code{cntry} column. The cache makes this cheap on repeat calls,
and \code{select} keeps the transfer small.
}
\examples{
\dontrun{
# Every Dutch respondent, all rounds.
nl <- ess_country("NL")
nl[, .N, by = essround]

# Two rounds, a few variables.
nl <- ess_country("NL", rounds = 10:11,
                  select = c("essround", "agea", "ppltrst"))
}

}
