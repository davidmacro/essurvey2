% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variables.R
\name{ess_variable_info}
\alias{ess_variable_info}
\title{Value labels and question wording for a variable}
\usage{
ess_variable_info(
  variable,
  round = NULL,
  kind = "integrated",
  variable_version = NULL,
  cache = TRUE
)
}
\arguments{
\item{variable}{A variable name such as \code{"ppltrst"}, or a catalogue
variable ID.}

\item{round}{An ESS round number, used to locate the variable when a name is
given.}

\item{kind}{Which data file of that round, as named by \code{\link[=ess_file_kinds]{ess_file_kinds()}}.}

\item{variable_version}{The version to fetch, when \code{variable} is an ID.}

\item{cache}{If \code{TRUE} (the default), reuse catalogue answers for the rest
of the session.}
}
\value{
A list with elements \code{name}, \code{label}, \code{question}, \code{description} and
\code{codes}, the last being a \code{data.table} of \code{value}, \code{label} and
\code{is_missing}.
}
\description{
Returns a variable's response categories — the codes that appear in the data
and what they mean — along with the question as it was put to respondents.
}
\details{
Parquet files carry codes, not labels, so this is where the labels live. The
\code{is_missing} column flags the codes the ESS treats as missing, which is what
\code{recode_missings = TRUE} acts on and what \code{\link[=ess_recode_missings]{ess_recode_missings()}} uses.
}
\examples{
\dontrun{
info <- ess_variable_info("ppltrst", round = 11)
info$question
info$codes

# The codes the ESS counts as missing.
info$codes[is_missing == TRUE]
}

}
