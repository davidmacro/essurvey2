% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{ess_download_file}
\alias{ess_download_file}
\title{Download an ESS data file to disk}
\usage{
ess_download_file(
  doi,
  path = NULL,
  format = "parquet",
  recode_missings = TRUE,
  user_id = ess_user_id(),
  use_cache = TRUE,
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{doi}{The DOI of a data file, as listed by \code{\link[=ess_data_files]{ess_data_files()}}. Study
DOIs are not accepted by the API.}

\item{path}{Where to write the file. When \code{NULL} (the default) the cache
location is used if caching is on, otherwise a temporary file.}

\item{format}{One of \code{"parquet"} (the default), \code{"csv"}, \code{"sav"} or
\code{"dta"}.}

\item{recode_missings}{If \code{TRUE} (the default), the API converts values
flagged as missing in the metadata into system missing values. See
\code{\link[=ess_data_file_url]{ess_data_file_url()}} for why this cannot be switched off with a value.}

\item{user_id}{Your ESS API user ID. Defaults to \code{\link[=ess_user_id]{ess_user_id()}}.}

\item{use_cache}{If \code{TRUE} (the default) reuse a previously downloaded copy,
and keep the download for next time. See \code{\link[=ess_cache_dir]{ess_cache_dir()}}.}

\item{overwrite}{If \code{TRUE}, download again even if the file is already
cached at \code{path}.}

\item{quiet}{If \code{TRUE}, suppress progress and status messages.}
}
\value{
The path to the downloaded file, invisibly.
}
\description{
Fetches one data file from the ESS data API and writes it to disk without
parsing it. Use \code{\link[=ess_data]{ess_data()}} instead to get a \code{data.table} straight away, or
this function when you want the file itself.
}
\examples{
\dontrun{
# Needs a configured user ID; see ?ess_user_id.
path <- ess_download_file("10.21338/ess6e02_6")
file.size(path)

# Keep the SPSS file instead, in a directory of your choosing.
ess_download_file("10.21338/ess6e02_6", path = "ess6.sav", format = "sav")
}

}
