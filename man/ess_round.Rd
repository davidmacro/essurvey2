% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{ess_round}
\alias{ess_round}
\title{Download ESS rounds}
\usage{
ess_round(
  rounds,
  kind = "integrated",
  select = NULL,
  recode_missings = TRUE,
  format = "parquet",
  user_id = ess_user_id(),
  use_cache = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{rounds}{One or more round numbers. \code{\link[=ess_rounds]{ess_rounds()}} lists what is
published.}

\item{kind}{Which data file of each round to download, as named by
\code{\link[=ess_file_kinds]{ess_file_kinds()}}. Defaults to the main integrated file.}

\item{select}{Optionally, a character vector of variables to read. Only
these columns are decoded from the parquet file, which is considerably
faster than reading all of them. \code{\link[=ess_variables]{ess_variables()}} lists the names.}

\item{recode_missings}{If \code{TRUE} (the default), values flagged as missing in
the ESS metadata — "Not applicable", "Refusal", "Don't know" and so on —
arrive as \code{NA}. Set to \code{FALSE} to keep the original codes.}

\item{format}{The transfer format, \code{"parquet"} by default. See
\code{\link[=ess_data]{ess_data()}} for the alternatives.}

\item{user_id}{Your ESS API user ID. Defaults to \code{\link[=ess_user_id]{ess_user_id()}}.}

\item{use_cache}{If \code{TRUE} (the default), reuse downloads already on disk.}

\item{quiet}{If \code{TRUE}, suppress progress and status messages.}
}
\value{
A \link[data.table:data.table]{data.table::data.table}. When several rounds are requested they
are stacked with \code{\link[data.table:rbindlist]{data.table::rbindlist()}} and \code{fill = TRUE}, so a variable
absent from a round is \code{NA} for that round's rows. The \code{essround} column
distinguishes them.
}
\description{
Downloads one or more rounds of the European Social Survey and returns them
as a single \code{data.table}. The DOI of each round's data file is resolved from
the live metadata catalogue, so a newly published round or a new file edition
is picked up without updating this package.
}
\details{
Downloads are cached on disk, so calling this again — now or in a later
session — costs nothing. See \code{\link[=ess_cache_dir]{ess_cache_dir()}}.
}
\examples{
\dontrun{
# One round, all variables.
dt <- ess_round(11)
dim(dt)

# Two rounds, a handful of variables.
dt <- ess_round(10:11, select = c("essround", "cntry", "agea", "ppltrst"))
dt[, .(mean_trust = mean(ppltrst, na.rm = TRUE)), by = .(essround, cntry)]

# The self-completion file of round 11.
sc <- ess_round(11, kind = "self_completion")
}

}
