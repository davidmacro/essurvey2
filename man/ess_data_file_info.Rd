% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{ess_data_file_info}
\alias{ess_data_file_info}
\title{Details of an ESS data file}
\usage{
ess_data_file_info(datafile_id, datafile_version, cache = TRUE)
}
\arguments{
\item{datafile_id}{One or more data file IDs, as returned by
\code{\link[=ess_data_files]{ess_data_files()}} or \code{\link[=ess_rounds]{ess_rounds()}}.}

\item{datafile_version}{Matching versions. The catalogue requires a version,
so this is not optional; \code{\link[=ess_data_files]{ess_data_files()}} returns it alongside the ID.}

\item{cache}{If \code{TRUE} (the default), reuse catalogue answers for the rest
of the session.}
}
\value{
A \code{data.table} with one row per data file and columns \code{file_label},
\code{description}, \code{edition}, \code{doi}, \code{doi_url}, \code{published}, \code{n_variables},
\code{n_countries}, \code{countries}, \code{datafile_id} and \code{datafile_version}. The
\code{countries} column is a list column of ISO-2 codes.
}
\description{
Resolves catalogue IDs to the information needed to download a file — above
all its DOI — plus the countries it covers and how many variables it has.
}
\details{
Accepts vectors, and resolves them in a single catalogue request.
}
\examples{
\dontrun{
r <- ess_rounds()
info <- ess_data_file_info(r$datafile_id, r$datafile_version)
info[, .(file_label, doi, n_countries)]
}

}
