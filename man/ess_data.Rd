% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{ess_data}
\alias{ess_data}
\title{Read an ESS data file into a data.table}
\usage{
ess_data(
  doi,
  format = "parquet",
  recode_missings = TRUE,
  select = NULL,
  user_id = ess_user_id(),
  use_cache = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{doi}{The DOI of a data file, as listed by \code{\link[=ess_data_files]{ess_data_files()}}. Study
DOIs are not accepted by the API.}

\item{format}{One of \code{"parquet"} (the default), \code{"csv"}, \code{"sav"} or
\code{"dta"}.}

\item{recode_missings}{If \code{TRUE} (the default), the API converts values
flagged as missing in the metadata into system missing values. See
\code{\link[=ess_data_file_url]{ess_data_file_url()}} for why this cannot be switched off with a value.}

\item{select}{Optionally, a character vector of column names to read. For
parquet only the requested columns are decoded, which is much faster than
reading all 600-plus variables of an integrated file. Unknown names are an
error.}

\item{user_id}{Your ESS API user ID. Defaults to \code{\link[=ess_user_id]{ess_user_id()}}.}

\item{use_cache}{If \code{TRUE} (the default) reuse a previously downloaded copy,
and keep the download for next time. See \code{\link[=ess_cache_dir]{ess_cache_dir()}}.}

\item{quiet}{If \code{TRUE}, suppress progress and status messages.}
}
\value{
A \link[data.table:data.table]{data.table::data.table}.
}
\description{
Downloads a data file by DOI and reads it into memory. Parquet — the
default, and the format the API is fastest at serving — is read with
\pkg{arrow}; \code{csv} with \code{\link[data.table:fread]{data.table::fread()}}; \code{sav} and \code{dta} with
\pkg{haven}, which must be installed for those.
}
\details{
\code{\link[=ess_round]{ess_round()}} and \code{\link[=ess_country]{ess_country()}} are usually more convenient: they find the
DOI for you. Use \code{ess_data()} for files those do not cover, such as the
contact forms, or for data outside the main ESS series.
}
\examples{
\dontrun{
# The whole ESS round 6 integrated file.
dt <- ess_data("10.21338/ess6e02_6")
dim(dt)

# Just a few variables, which is far quicker.
dt <- ess_data("10.21338/ess6e02_6", select = c("cntry", "agea", "gndr"))
dt[, .N, by = cntry]
}

}
