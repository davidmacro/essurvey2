% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gql.R
\name{ess_gql}
\alias{ess_gql}
\title{Query the ESS metadata catalogue}
\usage{
ess_gql(query, variables = list(), cache = TRUE)
}
\arguments{
\item{query}{A GraphQL query document.}

\item{variables}{A named list of query variables.}

\item{cache}{If \code{TRUE} (the default), reuse the answer to an identical query
for the rest of the session. Pass \code{FALSE} to force a fresh request.}
}
\value{
The \code{data} element of the response, as a nested list.
}
\description{
Sends a GraphQL query to the catalogue behind the ESS data portal and returns
the parsed \code{data} payload. The catalogue needs no user ID.
}
\details{
The \verb{ess_*} functions in this package cover the queries most users need.
\code{ess_gql()} is exported for the rest: the schema exposes considerably more
than is wrapped here, and it is introspectable.
}
\examples{
\dontrun{
ess_gql("
  query($id: ID!) {
    search {
      seriesMetadata(id: $id, instance: PUBLISHED, agencyId: INT_ESSERIC) {
        title { en }
      }
    }
  }",
  variables = list(id = ess_series_id())
)
}

}
