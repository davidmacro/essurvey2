% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missings.R
\name{ess_recode_missings}
\alias{ess_recode_missings}
\title{Recode ESS missing values to NA}
\usage{
ess_recode_missings(
  dt,
  round = NULL,
  variables = NULL,
  kind = "integrated",
  cache = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{dt}{A \code{data.table}, normally from \code{\link[=ess_round]{ess_round()}}.}

\item{round}{The ESS round the data came from. When \code{NULL} (the default) it
is taken from the \code{essround} column.}

\item{variables}{Optionally, restrict recoding to these columns. Defaults to
every column of \code{dt}.}

\item{kind}{Which data file of the round, as named by \code{\link[=ess_file_kinds]{ess_file_kinds()}}.}

\item{cache}{If \code{TRUE} (the default), reuse catalogue answers for the rest of
the session.}

\item{quiet}{If \code{TRUE}, suppress the progress bar and the summary.}
}
\value{
\code{dt}, invisibly and modified in place.
}
\description{
Converts the codes the ESS flags as missing into \code{NA}, one variable at a
time, using each variable's own missing codes from the metadata catalogue.
}
\details{
Asking the API to do this instead — the default \code{recode_missings = TRUE} of
\code{\link[=ess_round]{ess_round()}} and \code{\link[=ess_data]{ess_data()}} — is faster and needs no metadata requests.
Use this function for data fetched with \code{recode_missings = FALSE}, when you
want the original codes for some variables and \code{NA} for others, or when you
need to see which codes were affected.

The table is modified \strong{by reference}, in keeping with \code{data.table}
semantics: the object passed in is changed, and returned invisibly so that
calls can be chained. Pass \code{data.table::copy(dt)} to leave the original
alone.
}
\examples{
\dontrun{
# Fetch the original codes, then recode a few variables locally.
dt <- ess_round(11, select = c("essround", "cntry", "agea", "ppltrst"),
                recode_missings = FALSE)
dt[, .(max_age = max(agea))]         # 999, the "not available" code

ess_recode_missings(dt)
dt[, .(max_age = max(agea, na.rm = TRUE))]
}

}
